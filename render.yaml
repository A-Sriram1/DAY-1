services:
  - type: web
    name: car-etl-dashboard
    runtime: python
    buildCommand: pip install -r backend/requirements.txt
    startCommand: gunicorn backend.app:app
    plan: free
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
